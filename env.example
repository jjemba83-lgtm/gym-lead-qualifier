# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DATABASE_URL=sqlite:///db.sqlite3

# LLM API Keys
GROK_API_KEY=your_grok_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Gmail API (credentials file should be in secrets/gmail_credentials.json)
# No environment variable needed - using file-based auth

# Sales Team Notifications
SALES_TEAM_EMAIL=stephen.l.roberts20@gmail.com
# Future: Add more emails separated by commas
# SALES_TEAM_EMAILS=email1@example.com,email2@example.com

# Business Hours (24-hour format, timezone-aware)
BUSINESS_HOURS_START=9   # 9 AM
BUSINESS_HOURS_END=20    # 8 PM
BUSINESS_TIMEZONE=America/New_York

# Message Delays (in minutes)
MESSAGE_DELAY_MIN=5      # Minimum delay before sending
MESSAGE_DELAY_MAX=10     # Maximum delay (random between min and max)

# Test Mode - bypasses all delays and business hour restrictions
TEST_MODE=True           # Set to False in production

# Hot Lead Thresholds
HOT_LEAD_SCORE_THRESHOLD=0.7  # Score above this triggers hot lead notification
WARM_LEAD_SCORE_THRESHOLD=0.5  # Score above this gets priority (future use)

# WhatsApp Configuration (Phase 2)
# WHATSAPP_PHONE_NUMBER=+1234567890  # Your WhatsApp number for notifications
# WHATSAPP_TEST_MODE=True             # Use pywhatkit (WhatsApp Web) vs API

# Zapier Webhooks (Phase 3)
# ZAPIER_CLUBREADY_WEBHOOK=https://hooks.zapier.com/hooks/catch/xxxxx/yyyyy/
# ZAPIER_TEST_MODE=True               # Log webhook calls without sending

# Twilio Configuration (Future)
# TWILIO_ACCOUNT_SID=your_account_sid_here
# TWILIO_AUTH_TOKEN=your_auth_token_here
# TWILIO_PHONE_NUMBER=+1234567890
# TWILIO_TEST_MODE=True               # Log SMS without sending
